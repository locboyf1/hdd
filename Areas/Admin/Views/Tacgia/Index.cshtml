@using Company.Areas.Admin.Models
@model IList<Tacgia>

@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
    <main id="main" class="main">
        <div class="pagetitle">
            <h2>Danh sách các chức năng</h2>

            <p>
                <a type="button" class="btn btn-success" asp-area="Admin" asp-controller="Tacgia" asp-action="Create">
                <i class="bi bi-file-earmark-text me-1"></i>Thêm tác giả</a>
                <section class="section dashboard">
                    <div class="row">
                        <div class="col-12">
                            <div class="card recent-sales overflow-auto">
                                <div class="card-body mt-4">
                                    <table class="table table-borderless datatable">
                                        <thead>
                                            <tr>
                                                <th class="col-1 text-center">#</th>
                                                <th class="col-2 text-center">Tên tác giả</th>
                                                <th class="col-2 text-center">Năm sinh</th>
                                                <th class="col-2 text-center">Quê quán</th>
                                                <th class="col-2 text-center">Chức năng</th>
                                             </tr>
                                        </thead>
                                        <tbody>
                                            @foreach(var item in Model)
                                            {
                                                <tr>
                                                    <td class="text-center" scope="row">@item.Matacgia</td>
                                                    <td class="text-left text-primary" scope="row"><a asp-area="Admin"
                                                        asp-asp-controller="Tacgia" asp-action="Details"
                                                        asp-route-itemid="@item.Matacgia">@item.Tentacgia</a></td>
                                                    <td class="text-center" scope="row">@item.Namsinh</td>
                                                    <td class="text-center" scope="row">@item.Quequan</td>
                                                    <td class="text-center" scope="row">
                                                        <a asp-area="Admin" asp-controller="Tacgia" asp-action="Edit"
                                                            asp-route-id="@item.Matacgia" class="btn btn-primary btn-sm"
                                                            title="Sửa tác giả"><i class="bi bi-pencil"></i></a>
                                                        <a asp-area="Admin" asp-controller="Tacgia" asp-action="Delete"
                                                            asp-route-id="@item.Matacgia" class="btn btn-danger btn-sm"
                                                            title="Xóa tác giả"><i class="bi bi-trash"></i></a>
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </p>
        </div>
    </main>
